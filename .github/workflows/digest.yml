name: Daily Digest

on:
  schedule:
    - cron: '30 7 * * 1-5'   # 15:30 北京时间（收盘后），周一至周五
  workflow_dispatch:
    inputs:
      demo:
        description: '发送演示邮件（测试分级格式）'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      TZ: Asia/Shanghai
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Send digest
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_TO: ${{ secrets.SMTP_TO }}
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }}
        run: |
          if [ "${{ github.event.inputs.demo }}" = "true" ]; then
            python3 digest.py --demo
          else
            python3 digest.py
          fi

      - name: Commit cleared digest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/digest.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "无变更"
          else
            git commit -m "chore: 清空异动日报"
            git pull --rebase origin main 2>/dev/null || true
            git push
          fi
